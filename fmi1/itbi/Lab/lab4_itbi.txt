2.

echo "Introduceți trei numere întregi:"
read num1 num2 num3
sum=$((num1 + num2 + num3))
echo "Suma este: $sum"

3.

nume=$(whoami)
echo "Numele meu de utilizator este: $nume"

4.

gcc hello.c -o hello && ./hello

5.

#include <stdio.h>

int main() {
    char name[50];
    printf("Introduceți numele dumneavoastră: ");
    scanf("%s", name);
    printf("Salut, %s!\n", name);
    return 0;
}

6.
#include <stdio.h>

int main(int argc, char *argv[], char *envp[]) {
    for (int i = 0; envp[i] != NULL; i++) {
        printf("%s\n", envp[i]);
    }
    return 0;
}

gcc printenv.c -o printenv && ./printenv

7.

mkdir -p ~/bin
cp hello ~/bin/
export PATH=$PATH:~/bin
hello

